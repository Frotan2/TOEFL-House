# Technical Reference

**STATUS: SUPPORTING EVIDENCE — NOT PRIMARY NORMATIVE AUTHORITY**

Technical reference documents here exist to preserve evidence that would make the canonical set unwieldy.

- `current-state-compliance-evidence.md` — evidence-backed reconciliation of implementation against documented target state.

Use the canonical root documents for current rules. Use `history/` for historical provenance.
